defaults: &defaults
  migrations: db/migrations
  adapter: postgres
  max_connections: 5
  host: localhost
  user: postgres

development:
  <<: *defaults
  password:
  database: app_development

test:
  <<: *defaults
  password:
  database: app_test

staging:
  <<: *defaults
  password:
  database: app_staging

production:
  <<: *defaults
  password:
  database: app
